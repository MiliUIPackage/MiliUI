# 更新記錄

套組本體的版本號是發佈日期（`YYYYMMDD`，寫在 `AddOns/MiliUI/MiliUI.toc` 的 `## Version`），
更新後第一次登入會在聊天視窗提示一次。各插件另有自己的 SemVer 版本。

> 給發佈者：玩家看的版本可以用 `miliui-release-notes` 技能從 git tag 區間產生
> （繁中／簡中／英文三份）。這份檔案只收「玩家會注意到的變化」，內部重構不必列。
>
> 2026-08-28 才開始寫，更早的變更請看 git 歷史。

---

## 未發佈

### 新增

- **`/miliui taint on`**：把遊戲的污染記錄（`taintLog 2`）常駐開啟，之後每次登入都自動開，
  直到打 `/miliui taint off` 為止。回報「戰鬥中遭到封鎖」這類問題時用得到——
  記錄寫在 `Logs\taint.log`，重載介面會清空重寫，要留存先把檔案複製走。
  `/miliui taint` 可以看目前狀態。

---

## 20260910

### 新增

- **米利的採購清單**（新插件，`/mlist`）：專業用的採購清單。在製作頁或代工下單頁把配方
  加進清單、指定要做幾份，材料需求跟著相乘；清單算出還缺什麼（背包與銀行分開列，郵件
  在途的量也算），到拍賣場可以直接搜尋並購買，單買或整批買都行。星數（品質）可以指定，
  商店買得到的材料會自動排除，不想買的可以右鍵忽略。
  預設一收到報價就成交；想先看價格的話到 `/mlist config` 打開「購買前要我確認」，
  還能設定只有總價超過某個金額才停下來問。天價保險攔下來的那幾筆一律會問，關不掉。
- **Cell「隊伍目標」**（預設關，Cell →「工具 → 隊伍目標」）：隊伍框每一格旁邊多一顆按鈕，
  顯示那個隊員正在打的目標，左鍵點一下就選取它。順序、外觀、點擊施法都跟著隊伍版面走。
  血條依敵我與精英類型上色（預設值就是套組姓名板那一套，十個色票都可以自己調），
  在野外還會把「還欠你進度的怪」另外上色，並顯示團隊標記。
- **單位框架**：新增「首領的目標」與「寵物的目標」兩個單位框，都預設不啟用
  （`/muf` → 單位）。首領的目標打開之後預設只顯示副本裡重大的那些光環。
- **滑鼠淡出**：專注標記列與爆發藥水列可以設成平常淡出、滑鼠靠近才顯示，淡出程度可調。

### 修正

- **團隊標記在 Cell 的框架上完全不顯示**（12.1 之後就一直是這樣）。現在隊伍／團隊框
  和新的隊伍目標按鈕都畫得出那八個標記。
- **Cell 滑鼠點擊施法**：套組預設改成每個專精各一份設定檔，換專精不會再共用同一套。
- **專注目標助手**：在 M+ 計時中，打斷宣告送不出去。
- **Cell**：按下準備確認時可能出錯。

### 調整

- **任務追蹤器**：「導航中」的說明太長，縮成一句。

---

## 20260908

### 修正

- **戰鬥中隨機跳出「遭到封鎖」**：按巨集、快捷列技能轉冷卻的時候會冒出錯誤視窗。
  來源是資訊列處理暴雪教學提示（那些黃色泡泡）的方式，已經改成不去動它們的位置。

---

## 20260907

### 新增

- **整個套組的框體互相磁吸**：資訊列、傷害統計、小地圖、任務追蹤器、聊天列、專注標記列、
  爆發藥水列，拖到相差 2 像素內就會貼齊對方的邊。傷害統計的第二個視窗貼上主視窗之後，
  拖主視窗會帶著它一起走。
- **小地圖**：支援編輯模式拖曳；公會／好友名單改成可以互動的面板（滑到名單上它會留著，
  左鍵人名密語、右鍵邀請組隊），插件按鈕收納袋改成滑過去就開。原本的「資訊列」
  在設定裡改叫「社交列」，跟資訊列插件區分開。
- **資訊列**：延遲區塊滑過去會同時顯示本地與世界延遲並解釋差別，右鍵可以選條上要放哪一個
  （預設世界）。
- **光環美化**：堆疊層數的文字大小與描邊可以調了。

### 修正

- **快捷聊天列**：離開戰鬥時可能崩潰，或是聊天列突然失去位置跑到別的地方。
- **單位框架**：顯示條件（例如「只在隊伍中顯示」）在戰鬥中不生效。
- **探究標記按鈕**：站在敦敦的範圍外按下去會標到原本的目標。
- **資訊列**：從大寶庫領到的鑰石，戰隊資訊那邊抓不到。

### 效能

- **資訊列**：停靠模式原本每次換區與載入畫面都會把整個介面的版面重算一次（一場登入
  十幾次、每次上百毫秒），現在只有畫面真的被遊戲重設時才重貼。

---

## 20260906

### 新增

- **資訊列**：新增「戰隊資訊」區塊。方塊上顯示目前角色的鑰石，左鍵展開戰隊所有角色的
  鑰石、本週寶庫進度、探究懸賞圖與鍍金儲物箱（裝有 Syndicator 時多一欄虛無之核），
  右鍵可把所有角色的鑰石發到隊伍。面板貼著方塊開，資訊列在畫面上方時會改成往下展開。
  這組功能原本掛在 KeystoneLoot 視窗旁邊（套組本體的「角色鑰石記錄」），現在搬進
  資訊列並從本體移除；舊記錄會在第一次登入時自動搬過來。
- **資訊列**：新增停靠模式（`/mib` → 一般）。貼上緣或下緣、填滿整個寬度，並把介面往內
  縮一條把其他元素推開，關掉就還原。停靠時的內容可以選置中／靠左／靠右（預設置中）。
- **任務追蹤器**：內建傳奇鑰石計時面板（`/mquest` → 傳奇鑰石）。計時（含 +2／+3）、
  鑰石等級與詞綴、死亡數與損失時間、首領與擊殺時間、敵軍進度條。鑰石進行中，
  任務清單摺起來的位置就給它。**套組因此不再收錄 WarpDeplete**，兩邊留著會疊成兩層；
  舊資料夾還在的話登入時會提醒一次，要不要刪由你決定。
- **任務追蹤器**：M+ 進行中自動收起任務清單改由它負責，預設開啟。
- **設定視窗**：新增「便利功能」分頁，把不針對特定介面的設定從「插件強化」搬過來
  （星雲之核骰裝隱藏、遊戲行為、登入訊息、舊插件相容、探究標記鈕、商人自動化）。
- **單位框架**：目標框加上觀察按鈕。戰鬥中預設不顯示，也點不動（設定可關）。
- **磁吸**：專注標記列與爆發藥水列會互相吸附，拖前面那條會帶著後面那條走。
- **傷害統計**：智慧顯示改成看游標——脫戰後若滑鼠還停在任一個視窗上就先不切回總計，
  離開才切。
- **自動接任務**：內建的排除清單補上三條「至暗之夜」週任。

### 修正

- **單位框架**：團滅那種一瞬間很多人掉血的場合，死掉的目標血條可能永久停在死前的數值
  （文字顯示「已死亡」但條還是滿的）。
- **快捷聊天列**：目標是跨服或不同隊伍的玩家時，按密語按鈕會出錯。現在那種情況改成
  開一個空的 `/w `，按 Tab 一樣補得到名字。
- **專注目標助手**：斷法者是受限身分的單位時，施法條可能出錯。
- **隊友鑰石通報**：M+ 計時中或首領戰進行中，遊戲會封鎖插件之間的通訊，
  導致同一份鑰石清單被每個裝了套組的隊友各貼一次。那些場合改成不回應。
- **任務追蹤器**：M+ 的敵軍進度算錯（滿了顯示成 14.58%），進度條也不會轉綠。
- **冷卻管理器**（Ayije_CDM 強化）：增益長條的圖示偏移出方框、層數數字被裁掉、
  召喚物的長條有一秒沒有名字。
- **冒險指南天賦比較／嗜血音樂／爆發藥水助手**：補上 12.1.5 的版本號，
  不再顯示「過期」。

### 效能

- **施法條延遲條**（冷卻管理器強化）：原本不論有沒有在施法都每一幀跑一次，
  現在只在施法期間運作。
- **滑鼠提示**：血條與目標行的輪詢收成一支共用計時器，而且只在畫面上真的有提示時才跑。

### 調整

- **專注目標**：`MiliUI_Focus` 整支的用詞從「焦點」正名為暴雪官方的「專注目標」，
  包含打斷宣告的預設句子（自己改過宣告內容的不受影響）。
- **登入訊息**：改成「版本號變動後的第一次登入」才印一次（不再每次登入都出現），
  網址改成可以直接複製的純文字，並且在 `/miliui` →「便利功能」裡可以關掉。
  非中文客戶端也看得到英文版本。
