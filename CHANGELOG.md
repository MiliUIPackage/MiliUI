# 更新記錄

套組本體的版本號是發佈日期（`YYYYMMDD`，寫在 `AddOns/MiliUI/MiliUI.toc` 的 `## Version`），
更新後第一次登入會在聊天視窗提示一次。各插件另有自己的 SemVer 版本。

> 給發佈者：玩家看的版本可以用 `miliui-release-notes` 技能從 git tag 區間產生
> （繁中／簡中／英文三份）。這份檔案只收「玩家會注意到的變化」，內部重構不必列。
>
> 2026-08-28 才開始寫，更早的變更請看 git 歷史。

---

## 未發佈

### 新增

- **資訊列**：新增「戰隊資訊」區塊。方塊上顯示目前角色的鑰石，左鍵展開戰隊所有角色的
  鑰石、本週寶庫進度、探究懸賞圖與鍍金儲物箱（裝有 Syndicator 時多一欄虛無之核），
  右鍵可把所有角色的鑰石發到隊伍。面板貼著方塊開，資訊列在畫面上方時會改成往下展開。
  這組功能原本掛在 KeystoneLoot 視窗旁邊（套組本體的「角色鑰石記錄」），現在搬進
  資訊列並從本體移除；舊記錄會在第一次登入時自動搬過來。

### 修正

- **單位框架**：團滅那種一瞬間很多人掉血的場合，死掉的目標血條可能永久停在死前的數值
  （文字顯示「已死亡」但條還是滿的）。
- **快捷聊天列**：目標是跨服或不同隊伍的玩家時，按密語按鈕會出錯。現在那種情況改成
  開一個空的 `/w `，按 Tab 一樣補得到名字。
- **焦點助手**：斷法者是受限身分的單位時，施法條可能出錯。
- **隊友鑰石通報**：M+ 計時中或首領戰進行中，遊戲會封鎖插件之間的通訊，
  導致同一份鑰石清單被每個裝了套組的隊友各貼一次。那些場合改成不回應。
- **冒險指南天賦比較／嗜血音樂／爆發藥水助手**：補上 12.1.5 的版本號，
  不再顯示「過期」。

### 效能

- **施法條延遲條**（冷卻管理器強化）：原本不論有沒有在施法都每一幀跑一次，
  現在只在施法期間運作。
- **滑鼠提示**：血條與目標行的輪詢收成一支共用計時器，而且只在畫面上真的有提示時才跑。

### 調整

- **登入訊息**：改成「版本號變動後的第一次登入」才印一次（不再每次登入都出現），
  網址改成可以直接複製的純文字，並且在 `/miliui` →「插件強化」裡可以關掉。
  非中文客戶端也看得到英文版本。
